version: 2.1

# Define the jobs we want to run for this project
jobs:
  runner:
    machine: true
    resource_class: crowley/ubunut-init
    steps:
      - run: echo "Hi I'm on Runners!"

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - runner
